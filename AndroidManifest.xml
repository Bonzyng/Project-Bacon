<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.filos.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="22" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.filos.app.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.filos.oldclasses.PickerActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.filos.oldclasses.SelectedUserActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.LoginActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.filos.oldclasses.UserListActivity"
            android:label="@string/title_activity_user_list" >
        </activity>
        <activity
            android:name="com.filos.oldclasses.MatchedUserActivity"
            android:label="@string/title_activity_matched_user" >
        </activity>
        <activity 
            android:name="com.filos.app.FilosResultsActivity"
            android:label="@string/title_activity_filos_results" >
        </activity>
    </application>

</manifest>